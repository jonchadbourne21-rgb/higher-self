# MIRRORED-PARITY-P0.1B — Active Runtime Independence & Infrastructure Reconciliation

Status: **OPEN / CORE_AUTH_LLM_CANDIDATE_STAGED**

Build 4: **HOLD**.

Staged in the first tranche:
- native Sign in with Apple remains the only native login entry;
- server request authentication uses Mirrored JWT cookie/Bearer tokens only;
- legacy Manus auth fallback, SDK and Manus auth types are removed from runtime;
- canonical `invokeLLM()` now requires `OPENAI_API_KEY` and calls OpenAI directly;
- the canonical LLM path has no Forge fallback;
- legacy OAuth URL construction is removed from client auth paths;
- canonical landing/auth-error entry routes native users through Apple AuthenticationServices;
- Manus Vite runtime/debug collector is removed from the client build configuration;
- Manus debug assets are removed from `client/public`.

Not yet closed in P0.1B:
- legacy Forge-backed utility modules (storage/maps/data API/image/transcription) must be classified as retained+replaced or dead+removed;
- scheduled endpoints still assume Manus heartbeat headers and need an independent authenticated scheduler contract;
- owner-notification helper still needs classification/replacement;
- package-level unused Manus build dependency cleanup remains;
- real Railway deployment/network proof remains;
- full source/runtime dependency audit remains.

No production deploy, data write, TestFlight build, or Build 4 is authorized by this candidate.
